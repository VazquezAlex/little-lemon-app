/**
 * Confirmed booking page.
 * 
 * @returns { JSX.Element }
 */
const ConfirmedBooking = () => {

    return (
        <main>
            Confirmed Booking
        </main>
    );
}

export default ConfirmedBooking;
