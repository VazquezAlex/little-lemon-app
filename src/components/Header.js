import Nav from "./Nav/Nav";

/**
 * Page Header.
 * 
 * @returns { JSX.Element }
 */
const Header = () => {

    return (
        <header>
            <Nav />
        </header>
    );
}

export default Header;
