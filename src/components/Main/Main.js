import Hero from "./Hero";

/**
 * Main content for the page.
 * 
 * @returns { JSX.Element }
 */
const Main = () => {

    return (
        <main>
            <Hero />
        </main>
    );
}

export default Main;
