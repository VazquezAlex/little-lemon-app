import Hero from "./Hero";
import Specials from "./Specials";

/**
 * Main content for the page.
 * 
 * @returns { JSX.Element }
 */
const Main = () => {

    return (
        <main>
            <Hero />
            <Specials />
        </main>
    );
}

export default Main;
